apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.global.namespace }}
    date: {{ now | htmlDate }}
    version: {{ .Values.global.version | quote }}
