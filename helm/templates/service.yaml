apiVersion: v1
kind: Service
metadata:
  name: {{ include "exmple_app.fullname" . }}
  labels:
    {{- include "exmple_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exmple_app.selectorLabels" . | nindent 4 }}
