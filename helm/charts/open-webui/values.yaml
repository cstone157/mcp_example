# # ------------------------------------------------------------------------------
# # Webui :
# # ------------------------------------------------------------------------------
name: open-webui
image: dyrnq/open-webui:v0.3.16-ollama # ghcr.io/open-webui/open-webui:main
imagePullPolicy: IfNotPresent
replicas: 1
env:
  - name: OLLAMA_BASE_URL
    value: "http://ollama-service.open-webui.svc.cluster.local:11434"
service_type: NodePort
service:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30000

# image:
#   repository: ghcr.io/open-webui/open-webui
#   tag: main
#   pullPolicy: IfNotPresent

# service:
#   type: ClusterIP
#   port: 80 # Exposed port
#   targetPort: 8080 # Internal container port

# persistence:
#   enabled: true
#   size: 5Gi
#   accessMode: ReadWriteOnce

# # Configuration environment variables
# config:
#   # This default assumes the release name is "my-app" and the subchart is "ollama"
#   # You will likely override this in the parent values.yaml
#   ollamaBaseUrl: "http://my-app-ollama:11434"