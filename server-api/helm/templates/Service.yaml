apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: mcp-example-webservice
  name: mcp-example-webservice
  namespace: mcp-example
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: mcp-example-webservice
